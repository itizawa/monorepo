{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap":"lerna bootstrap",
    "dev": "lerna run dev --parallel --stream --scope=@monorepo/{client,server}"
  },
  "devDependencies": {
    "eslint": "^8.11.0",
    "lerna": "^4.0.0",
    "prettier": "^2.6.0",
    "typescript": "^4.6.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
